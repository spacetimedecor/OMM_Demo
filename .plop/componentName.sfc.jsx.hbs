import * as React from "react";
import Styled{{ properCase name }} from "./{{ properCase name }}.styled";

const {{ properCase name }} = (props) => (
  <Styled{{ properCase name }} {...props}>
    {props.children}
  </Styled{{ properCase name }}>
);

export default {{ properCase name }};
